{
  "name": "server",
  "version": "1.0.0",
  "main": "src/index.ts",
  "author": "@lunochkin",
  "license": "MIT",
  "scripts": {
    "start": "ts-node src/index.ts",
    "dev": "DEBUG=*,-express*,-nodemon*,-snapdragon* nodemon --watch './src' -e ts -x 'ts-node' src/index.ts",
    "test": "yarn test:raw | tap-spec",
    "test:raw": "ts-node ./node_modules/.bin/tape ./test/before.ts ./src/**/*.test.ts ./test/after.ts",
    "createDb": "ts-node src/createDb.ts",
    "seed": "yarn task seed",
    "rpc": "ganache-cli",
    "lint": "tslint --project ./tsconfig.json ./src/**/*.ts",
    "task": "ts-node src/task.ts",
    "docker:build": "docker build -t dex-server .",
    "docker:run": "docker run --env-file=.env -e 'PSQL_HOST=host.docker.internal' -p 7001:7001 --rm -it dex-server",
    "docker:test": "yarn docker:builder && yarn docker:builder:test",
    "docker:builder": "docker build --target=builder -t dex-server-builder .",
    "docker:builder:test": "docker run -it dex-server-builder yarn test"
  },
  "dependencies": {
    "0x.js": "^2.0.6",
    "@types/web3": "^1.0.12",
    "awilix": "^4.0.1",
    "body-parser": "^1.18.3",
    "bunyan": "^1.8.12",
    "dotenv": "^6.1.0",
    "express": "^4.16.4",
    "keythereum": "^1.0.4",
    "pg": "^7.7.1",
    "ramda": "^0.26.1",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.5",
    "sift": "^7.0.1",
    "ts-node": "^7.0.1",
    "typeorm": "^0.2.9",
    "typescript": "^3.2.1",
    "web3": "^1.0.0-beta.36",
    "ws": "^6.1.2",
    "yargs": "^12.0.5"
  },
  "devDependencies": {
    "ganache-cli": "^6.2.3",
    "mock-socket": "^8.0.5",
    "nodemon": "^1.18.7",
    "sinon": "^7.1.1",
    "tap-spec": "^5.0.0",
    "tape": "^4.9.1",
    "tslint": "^5.11.0",
    "tslint-config-standard": "^8.0.1",
    "uuid": "^3.3.2"
  }
}
